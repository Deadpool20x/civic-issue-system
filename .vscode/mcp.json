{
  "inputs": [],
  "servers": {
    "serverNameOne": {
      // Configuration for the first server (e.g., local stdio)
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    },
    "serverNameTwo": {
      // Configuration for the second server (e.g., remote HTTP)
      "type": "http",
      "url": "https://mcp.context7.com/mcp"
    },
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"]
    }
  }
}
